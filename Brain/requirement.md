# Already covered if you ran:

# pip install boto3 strands-agents strands-agents-tools

boto3
strands-agents
strands-agents-tools

# New additions for Cedar (permissions) and OpenSearch (audit log)

cedar-python
opensearch-py
python-dotenv
